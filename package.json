{
  "name": "open-news",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter='*' dev",
    "dev:server": "bun run --filter=server dev",
    "dev:web": "bun run --filter=web dev"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
